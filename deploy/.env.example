# Prefer immutable digest references in production.
WEB_IMAGE_REF=ghcr.io/OWNER/minecraft-server-status-web:sha-REPLACE_WITH_COMMIT
MONITOR_IMAGE_REF=ghcr.io/OWNER/minecraft-server-status-monitor:sha-REPLACE_WITH_COMMIT

STATUS_DOMAIN=status.example.com
CONFIG_FILE=../config/server.yaml

# Required only when the enabled endpoint in server.yaml uses ${...} interpolation.
JAVA_SERVER_HOST=
BEDROCK_SERVER_HOST=

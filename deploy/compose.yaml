services:
  web:
    image: ghcr.io/fan1k413/minecraft-server-status-web:main
    pull_policy: always
    environment:
      DATABASE_URL: file:/data/status.db
      SERVER_CONFIG_PATH: /config/server.yaml
    ports:
      - "127.0.0.1:15879:3000"
    volumes:
      - status-data:/data
      - ../config/server.yaml:/config/server.yaml:ro
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/api/health/live').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    init: true
    stop_grace_period: 20s

  monitor:
    image: ghcr.io/fan1k413/minecraft-server-status-monitor:main
    pull_policy: always
    environment:
      DATABASE_URL: file:/data/status.db
      SERVER_CONFIG_PATH: /config/server.yaml
    volumes:
      - status-data:/data
      - ../config/server.yaml:/config/server.yaml:ro
    restart: unless-stopped
    init: true
    stop_grace_period: 20s

volumes:
  status-data:
